

<%= simple_form_for [@project, @comment]  do |f| %>
    <%= f.input :content, placeholder: "What are your ideas for the project?", class: "form-project" %>
    <%= f.button :submit,"Add Comment", class: "btn btn-primary"  %>
<% end %>
