
<%= simple_form_for(@project) do |f| %>
    <%= f.input :title, placeholder: "What do you want to project?" %>
    <%= f.input :description, placeholder: "Add a description of your project", as: :text %>
    <%= f.input :location, placeholder: "City" %>
    <%#= f.input :picture %>
    <%#= f.input :picture_cache, as: :hidden %>

    <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
